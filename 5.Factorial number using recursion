#include<stdio.h>
int factorial(int n);

	int main()
	{
		int fact,num;
		printf("enter a number:");
		scanf("%d",&num);
		fact=factorial(num);
		printf("factorial of %d is %d",num,fact);
		return 0;
	}
	int factorial(int n)
	{
		if(n==1)
		return 1;
		return n*factorial(n-1);
	}
